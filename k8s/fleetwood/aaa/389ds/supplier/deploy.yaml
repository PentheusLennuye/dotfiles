---
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: ldap
  name: ldap
  namespace: it
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ldap
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: ldap
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: ldap
                topologyKey: "kubernetes.io/hostname"
      volumes:
        - name: 389ds-config
          persistentVolumeClaim:
            claimName: 389ds-nfs-config-pvc
        - name: 389ds-data
          persistentVolumeClaim:
            claimName: 389ds-nfs-data-pvc
        - name: 389ds-tls
          secret:
            secretName: 389ds-tls
            items:
              - key: "server.key"
                path: "server.key"
              - key: "server.crt"
                path: "server.crt"
              - key: "ca.crt"
                path: "ca/ca.crt"
      imagePullSecrets:
        - name: regcred
      containers:
        - name: dirsrv
          env:
            - name: DS_DM_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ds-dm-password
                  key: password
            - name: DS_SUFFIX_NAME
              value: "dc=cummings-online,dc=ca"
            - name: HOST_FQDN
              value: "ldap.cummings-online.ca"
            - name: REPLICATION_ROLE
              value: supplier
            - name: REPLICATION_PEERS
              value: "ldap1.cummings-online.ca:fleetwood-agreement-ldap1,ldap2.cummings-online.ca:fleetwood-agreement-ldap2"
            - name: REPLICATION_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ds-dm-password
                  key: password
          image: docker.cummings-online.ca/389ds:0.2.48
          imagePullPolicy: Always
          ports:
            - containerPort: 389
            - containerPort: 636
          volumeMounts:
            - name: 389ds-data
              readOnly: false
              mountPath: /var/lib/dirsrv
            - name: 389ds-config
              readOnly: true
              mountPath: /etc/krb5.conf
              subPath: krb5.conf
            - name: 389ds-config
              readOnly: false
              mountPath: /etc/dirsrv/ds.keytab
              subPath: ds.keytab
            - name: 389ds-config
              readOnly: false
              mountPath: /etc/dirsrv/slapd-service
            - name: 389ds-tls
              readOnly: true
              mountPath: /data/tls
