---
- name: linux setup
  become: true
  block:
    - import_tasks: packages.yaml
    - import_tasks: configurations.yaml
    - name: Start gssproxy
      ansible.builtin.service:
        name: gssproxy
        enabled: true
        state: started
